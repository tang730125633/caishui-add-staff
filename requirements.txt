# 核心依赖（必需）
requests>=2.31.0          # HTTP请求
pandas>=2.0.0             # Excel读取
openpyxl>=3.1.0           # Excel格式支持

# 配置获取工具依赖（可选）
playwright>=1.40.0        # 浏览器自动化（用于auto_config_helper.py）

# 开发依赖（可选）
pytest>=7.0.0
pytest-playwright>=0.4.0
black>=23.0.0
flake8>=6.0.0
